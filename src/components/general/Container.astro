---
interface Props {
  element?: string
  className?: string
}

const { element = 'section', className = '' } = Astro.props

const styles = `container mx-auto px-[1rem] ${className}`

const Tag = element
---

<Tag class:list={styles}>
  <slot />
</Tag>

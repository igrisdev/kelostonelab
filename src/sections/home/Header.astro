---
import Container from '@components/general/Container.astro'
// import { Image } from 'astro:assets'
import img from './../../../public/pro.png'

const info = [
  {
    title: 'Free shipping',
    span: 'Free shipping',
    icon: '🚚',
  },
  {
    title: 'Fast delivery',
    span: 'Fast delivery',
    icon: '🚚',
  },
  {
    title: 'Payment methods',
    span: 'Payment methods',
    icon: '🚚',
  },
  {
    title: 'Payment methods',
    span: 'Payment methods',
    icon: '🚚',
  },
]
---

<header
  class='pt-[8vh] min-h-screen bg-base-200 flex flex-col gap-4 md:gap-0 mb-10 md:mb-0'
>
  <Container
    className='flex-1 grid grid-cols-1 md:grid-cols-2 gap-4 items-center place-content-center md:place-content-center'
  >
    <div class='flex items-center justify-center'>
      <!-- <Image src={img} alt='Inicio' /> -->
      <div class='max-w-[300px] h-[300px] relative md:max-w-full md:h-full'>
        <img src={img.src} alt='Indico' />
      </div>
    </div>

    <div class='flex flex-col gap-6 text-center md:text-left'>
      <div>
        <div class='mb-2'>5 estrellas</div>
        <p class='font-semibold md:text-xl mb-2'>Best Quality Products</p>
        <h1 class='font-semibold text-3xl md:text-6xl md:leading-[65px]'>
          Join The Organic Movement!
        </h1>
      </div>

      <p>
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Fugiat incidunt
        rerum facilis illo, repellendus blanditiis ess
      </p>

      <div>
        <button class='btn btn-success text-base-100 font-bold'
          >Añadir al carrito</button
        >
      </div>
    </div>
  </Container>

  <div class='bg-info-content'>
    <Container
      className='grid grid-cols-1 sm:grid-cols-2 py-3 lg:grid-cols-4 gap-4 place-content-center'
    >
      {
        info.map(item => (
          <div class='p-2 px-4 bg-base-content text-base-100 flex items-center gap-4 h-[9vh]'>
            <div>{item.icon}</div>
            <div>
              <h3>{item.title}</h3>
              <p>{item.span}</p>
            </div>
          </div>
        ))
      }
    </Container>
  </div>
</header>
